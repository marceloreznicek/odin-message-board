<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body>
    <% for(let i = 0; i < messages.length; i++) { %> 
      <h4> Message <%= (i + 1) %>:</h4>
      <p>Text: <%= messages[i].text %></p>
      <p>From <%= messages[i].user %></p>
      <p>At <%= messages[i].added.toLocaleString('en-US', {
        day: '2-digit',
        month: '2-digit',
        hour: '2-digit', 
        minute: '2-digit',
        hour12: false
       }) %></p>
    <% } %>
  </body>
</html>
